package main

import (
	"testing"
)

func TestBucket(t *testing.T) {
	/*
		b := NewBucket(3, 3, 3)
		ch0 := b.GetChannel()
		if b.used != 1 || ch0 == nil {
			t.Errorf("used: %d", b.used)
			t.FailNow()
		}
		ch1 := b.GetChannel()
		if b.used != 2 || ch1 == nil {
			t.Errorf("used: %d", b.used)
			t.FailNow()
		}
		ch2 := b.GetChannel()
		if b.used != 3 || ch2 == nil {
			t.Errorf("used: %d", b.used)
			t.FailNow()
		}
		ch3 := b.GetChannel()
		if b.used != 3 || ch3 == nil {
			t.Errorf("used: %d", b.used)
			t.FailNow()
		}
		b.PutChannel(ch0)
		if b.used != 2 {
			t.Errorf("used: %d", b.used)
			t.FailNow()
		}
		b.PutChannel(ch1)
		if b.used != 1 {
			t.Errorf("used: %d", b.used)
			t.FailNow()
		}
		b.PutChannel(ch2)
		if b.used != 0 {
			t.Errorf("used: %d", b.used)
			t.FailNow()
		}
		b.PutChannel(ch3)
		if b.used != 0 {
			t.Errorf("used: %d", b.used)
			t.FailNow()
		}
	*/
}
